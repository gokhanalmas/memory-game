import React from 'react';

import { Main } from '../styles/Styles';

import SettingsContent from '../componets/settings/SettingsContent';

const Settings = () => {
  return (
    <Main page="settings">
      <SettingsContent />
    </Main>
  );
};

export default Settings;
